/**
 * Phase 3 prep skeleton (flag-gated, inactive):
 * Plan validator v2.
 */

export async function validatePlanV2(_ctx) {
  return {
    ok: true,
    violations: [],
    reason: 'V2_SKELETON_INACTIVE'
  };
}

export default { validatePlanV2 };

