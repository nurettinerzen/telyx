/**
 * Phase 3 prep skeleton (flag-gated, inactive):
 * Response generation v2.
 */

export async function generateResponseV2(_ctx) {
  return {
    ok: true,
    response: '',
    reason: 'V2_SKELETON_INACTIVE'
  };
}

export default { generateResponseV2 };

