/**
 * Phase 3 prep skeleton (flag-gated, inactive):
 * Planner v2 entrypoint.
 */

export async function planV2(_ctx) {
  return {
    ok: true,
    plan: null,
    reason: 'V2_SKELETON_INACTIVE'
  };
}

export default { planV2 };

