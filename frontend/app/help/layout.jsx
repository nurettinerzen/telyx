import '@/styles/features.css';

export const metadata = {
  title: 'Help Center | Telyx',
  description: 'Telyx AI yardim merkezi — sikca sorulan sorular, hizli baslangic ve destek.',
};

export default function HelpLayout({ children }) {
  return children;
}
